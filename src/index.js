import React from 'react';
import ReactDOM from 'react-dom/client';  // React 18+에서는 이렇게 import해야 함
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);